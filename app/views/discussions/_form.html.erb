<%= form_with(model: discussion, local: true) do |form| %>
  <% if discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(discussion.errors.count, "error") %> prohibited this discussion from being saved:</h2>

      <ul>
      <% discussion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :topic_id %>
    <%= form.number_field :topic_id, id: :discussion_topic_id %>
  </div>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_field :question, id: :discussion_question %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
